using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class iTweenTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //iTween.ColorFrom(this.gameObject, new Color(1f,1f,1f), 3f);

        //iTween.FadeFrom(this.gameObject, 0f, 5f);
        //iTween.FadeFrom(this.gameObject, iTween.Hash("alpha", 0f, "time", 5f, "easetype", iTween.EaseType.easeInOutQuart));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
